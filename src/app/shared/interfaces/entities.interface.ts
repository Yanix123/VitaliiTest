// EEntityKey — centralised TanStack Query keys
export enum EEntityKey {
  ITEMS = 'items',
  ITEM = 'item',
  FAVORITES = 'favorites',
}

// ERoute — app route paths
export enum ERoute {
  HOME = '/',
  FAVORITES = '/favorites',
  LOGIN = '/login',
  REGISTER = '/register',
}
